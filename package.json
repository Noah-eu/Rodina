{
  "name": "famcall-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix frontend ci && npm --prefix backend ci || true",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build || true",
    "build:all": "npm run build:frontend && npm run build:backend",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "e2e:browsers": "npx playwright install --with-deps",
    "e2e": "npx playwright test -c tests/e2e/playwright.config.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "node-fetch": "^2.6.7",
    "@playwright/test": "^1.48.0"
  }
}
